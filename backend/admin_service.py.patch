# --- PATCH START: ensure /health exists ---
try:
    from fastapi import FastAPI
    from admin_service import app as _app  # hvis filnavnet er noe annet, juster
except Exception:
    from admin_service import app as _app  # fallback

@app.get("/health")
def health():
    return {"status": "healthy"}
# --- PATCH END ---
