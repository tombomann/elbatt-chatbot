name: "Auto AI-fix & PR"

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ai-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run AI-fix
        run: |
          # (her kunne du kalle Python-script som bruker OpenAI for å “fikse” filer)
          echo "Simulert AI-fiks"
      - name: Create PR
        uses: peter-evans/create-pull-request@v6
        with:
          branch: ai-auto-fix
          title: "AI AutoFix: PR"
          commit-message: "Automatisk AI-fiks commit"
