apiVersion: v1
kind: ResourceQuota
metadata:
  name: elbatt-quota
  namespace: elbatt
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "1Gi"
    limits.cpu: "1"
    limits.memory: "2Gi"
    persistentvolumeclaims: "2"
