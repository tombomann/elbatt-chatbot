version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
      - OTHER_ENV_VAR=${OTHER_ENV_VAR}
    restart: unless-stopped
